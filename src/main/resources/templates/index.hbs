
{{#partial "content"}}

    <h1> Heroes</h1>

    {{#if heroes}}
        {{#each heroes }}
            <p>{{ name }} <a href="/tasks/{{id}}">read more...</a></p>
        {{/each}}
    {{else}}
        <p>No Heroes!</p>
    {{/if}}
{{/partial}}

{{> layout.hbs}}
